import React from 'react';
import Html from '../components/html';

const HtmlSkeleton = {
  type: 'html',
  staticProperties: ['label', 'path', 'visibility', 'labelAction'],
  Component: Html,
};

export default HtmlSkeleton;
